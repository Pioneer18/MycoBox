/**
 * System Service
 * =========================
 * Description: provides extra functionality to the system controller
 */
const {
    sessionConfig,
    environmentConfig,
    environmentModel,
    systemStatus,
    overrides
} = require('../globals/globals');

module.exports = {

    manageEnvironment: () => {

    }
}